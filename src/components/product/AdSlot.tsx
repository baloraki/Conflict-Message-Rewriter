// Placeholder for future monetization via affiliate content or advertising.
// Do not add real tracking scripts without updating the privacy policy.

interface AdSlotProps {
  placement?: "home" | "after-delete" | "footer";
  className?: string;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function AdSlot({ placement = "home", className }: AdSlotProps) {
  // Currently disabled. Configure and enable when monetization is ready.
  return null;
}
