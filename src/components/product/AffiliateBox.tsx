// Placeholder for future affiliate content.
// Potential categories: journals, mindfulness apps, communication books,
// conflict resolution resources, therapy directories.
// Only add verified, relevant, and clearly-labeled affiliate content.

interface AffiliateBoxProps {
  category?: "journals" | "books" | "apps" | "therapy";
  className?: string;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function AffiliateBox({ category, className }: AffiliateBoxProps) {
  // Currently disabled. Placeholder only.
  return null;
}
